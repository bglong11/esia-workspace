# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
alembic==1.13.1

# Celery and Redis for async processing
celery==5.3.6
redis==5.0.1

# PDF processing (keep for fallback)
pdfplumber==0.10.3
pypdf==3.17.4

# Core extraction dependencies
dspy-ai==2.4.0
pandas==2.1.4
tqdm==4.66.1

# Utilities
python-dotenv==1.0.0
aiofiles==23.2.1

# Note: Docling should be installed separately with its own environment
# See https://github.com/DS4SD/docling for installation instructions
